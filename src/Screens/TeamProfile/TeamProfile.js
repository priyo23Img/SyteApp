import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TeamProfile = () => {
  return (
    <View>
      <Text>TeamProfile</Text>
    </View>
  )
}

export default TeamProfile

const styles = StyleSheet.create({})