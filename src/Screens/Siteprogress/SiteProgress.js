import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SiteProgress = () => {
  return (
    <View>
      <Text>SiteProgress</Text>
    </View>
  )
}

export default SiteProgress

const styles = StyleSheet.create({})